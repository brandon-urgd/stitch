# Basic dependencies only - heavy stuff is in the ultimate layer!
boto3>=1.40.0,<2.0.0
python-multipart>=0.0.6,<1.0.0

# Note: All the heavy dependencies (Pillow, OpenCV, scipy, numpy, etc.)
# are now provided by the Ultimate Badass Lambda Layer!
# This makes deployments MUCH faster! 🚀
